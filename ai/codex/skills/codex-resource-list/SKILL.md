---
name: codex-resource-list
description: プロジェクトの Codex リソース一覧を表示する。「Codex リソース一覧」「スキル一覧」「エージェント一覧」「フック一覧」「プロジェクトリソース確認」「何があるか確認」「リソースを見せて」などで起動。
---

# Codex Resource List

プロジェクトの Codex リソース一覧を表示します。

## Help

ユーザー入力に `--help` が含まれる場合、以下を表示して終了:

```text
/codex-resource-list - Codex リソース一覧

概要:
  プロジェクトの Codex リソース（スキル・エージェント・設定）を一覧表示する。

使用方法:
  /codex-resource-list [オプション]

オプション:
  --skills  スキルのみ表示
  --agents  エージェントのみ表示
  --help    このヘルプを表示

例:
  /codex-resource-list              # すべてのリソースを表示
  /codex-resource-list --skills     # スキルのみ表示
  /codex-resource-list --agents     # エージェントのみ表示
```

## 実行手順

### 1. 表示対象を決定

- 引数が指定されていれば引数を優先
- 引数がない場合は発話内容から `skills` / `agents` / `all` を判定

### 2. リソースを収集

- **スキル**: `.codex/skills/` 配下の `SKILL.md` を持つディレクトリ
- **エージェント**: `.codex/agents/` 配下の `.md` ファイル
- **設定**: `.codex/config.toml` の内容

### 3. 一覧を整形して表示

- スキル: 名前と説明
- エージェント: 名前と説明
- 設定: 主要セクションのサマリ

## 出力フォーマット

```markdown
## Codex リソース一覧

### スキル (N)

| スキル | 説明 |
| ------ | ---- |

### エージェント (N)

| エージェント | 説明 |
| ------------ | ---- |

### 設定

| セクション | 概要 |
| ---------- | ---- |
```

## 重要な注意事項

- 存在しないディレクトリやファイルはスキップ
- リソースが0件のときは「なし」と明示
- 詳細編集は行わない（一覧のみ）
