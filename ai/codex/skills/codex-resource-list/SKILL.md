---
name: codex-resource-list
description: プロジェクトの Codex リソース一覧を表示する。「Codex リソース一覧」「スキル一覧」「エージェント一覧」「設定一覧」「プロジェクトリソース確認」「何があるか確認」「リソースを見せて」などで起動。表示対象（スキル/エージェント/設定）は発話から判断する。
---

# Codex Resource List

プロジェクトの Codex リソース一覧を表示します。

## Help

ユーザー入力に `--help` が含まれる場合、以下を表示して終了:

```text
/codex-resource-list - Codex リソース一覧

概要:
  プロジェクトの Codex リソース（スキル・エージェント・設定）を一覧表示する。

使用方法:
  /codex-resource-list

オプション:
  --help  このヘルプを表示

表示対象の伝え方（フラグの代わり）:
  「スキル」「スキル一覧」      → スキルのみ表示
  「エージェント」「agent」     → エージェントのみ表示
  「設定」「config」            → 設定のみ表示
  指定なし                     → すべて表示

例:
  /codex-resource-list   # すべてのリソースを表示
  「スキル一覧を見せて」           # スキルのみ表示
  「エージェント一覧」             # エージェントのみ表示
```

## 実行手順

### 1. 表示対象を決定（フラグの代わり）

発話内容から `skills` / `agents` / `all` を判定する。明示がなければ `all`（すべて表示）とする。読み取り専用のため確認は不要。

### 2. リソースを収集

- **スキル**: `.codex/skills/` 配下の `SKILL.md` を持つディレクトリ
- **エージェント**: `.codex/agents/` 配下の `.md` ファイル
- **設定**: `.codex/config.toml` の内容

### 3. 一覧を整形して表示

- スキル: 名前と説明
- エージェント: 名前と説明
- 設定: 主要セクションのサマリ

## 出力フォーマット

```markdown
## Codex リソース一覧

### スキル (N)

| スキル | 説明 |
| ------ | ---- |

### エージェント (N)

| エージェント | 説明 |
| ------------ | ---- |

### 設定

| セクション | 概要 |
| ---------- | ---- |
```

## 重要な注意事項

- 存在しないディレクトリやファイルはスキップ
- リソースが0件のときは「なし」と明示
- 詳細編集は行わない（一覧のみ）
